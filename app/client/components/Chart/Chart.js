import React from 'react';
import styles from './Chart.css';

const Chart = () => (
  <div className={styles.container}>
    chart with number of requests tracked each day
  </div>
);

export default Chart;
